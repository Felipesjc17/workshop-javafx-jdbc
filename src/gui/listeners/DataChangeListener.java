package gui.listeners;

//classe com objeto responsável por emitir um evento quando o formulario for alterado e ira atualizar lista(padrão observer)
public interface DataChangeListener {

	void onDataChanged();// evento dispara quando os dados mudarem
	
	
}
